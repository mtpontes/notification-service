name: Orchestrator

on:
  push:
    branches: [ ]
  pull_request:
    branches: [ ]

jobs:

  build:
    uses: ./.github/workflows/build.yml
    secrets: inherit

  infra:
    needs: build
    uses: ./.github/workflows/infra.yml
    secrets: inherit
